# Demo Build Instructions

1. Open `example.FCStd` in FreeCAD to inspect model and TechDraw pages.
2. Run export workflow to generate 3MF, screenshots, BOM CSV, and PDF.
3. Validate outputs in `prints/` and `docs/`.
