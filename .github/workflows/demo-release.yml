---
name: Tagged Release

"on":
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: Optional tag name override for manual runs
        required: false
        type: string

permissions:
  contents: write

jobs:
  publish:
    uses: ebirn/freecad_tools/.github/workflows/publish-tagged-release.yml@main
    with:
      config_path: export_config.yml
      project_root: .
      gui_session: run
      dry_run: false
      release_tag: ${{ github.event.inputs.release_tag || '' }}
      bundle_name: demo-artifacts
      bundle_paths: >-
        README.md INSTRUCTIONS.md export_config.yml example.FCStd
      generated_paths: >-
        docs prints
