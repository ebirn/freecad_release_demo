---
name: Demo Nightly Release

"on":
  schedule:
    - cron: "17 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    uses: ebirn/freecad_tools/.github/workflows/reusable-publish-release.yml@main
    with:
      config_path: .freecad_tools/config.yml
      project_root: .
      gui_session: run
      dry_run: false
      is_rolling_release: true
      bundle_name: demo-artifacts-nightly
      bundle_paths: >-
        README.md INSTRUCTIONS.md .freecad_tools/config.yml example.FCStd
      generated_paths: >-
        docs prints
